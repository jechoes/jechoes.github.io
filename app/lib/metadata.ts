import { Metadata } from 'next';

export const generateMetadata = (title: string): Metadata => ({
  title,
  description:
    'Los Angeles based marimba and voice artist with musical roots in Boston.',
});
